# Cursor Rules for RoadReady

## 🎯 Project Context
**App:** RoadReady  
**Stack:** Next.js + Supabase + Vercel + Resend + Twilio  
**Stage:** MVP Development  
**User Level:** A (Vibe-coder: AI does everything, user guides/tests)

## 📋 Directives
1. **Master Plan:** Always read `AGENTS.md` first.
2. **Documentation:** Use `agent_docs/` for requirements, stack, patterns, testing.
3. **Incremental Build:** Build one small feature at a time. Include a short manual test checklist.
4. **No Linting Mode:** Do not act as a linter. If needed, suggest `npm run lint` and fix issues.
5. **Communication:** Be concise. Explain plan before implementing. Prefer “manual-first” over complex AI extraction.

## ⚠️ What NOT To Do
- Do NOT delete files without explicit confirmation
- Do NOT modify database schemas without a migration plan
- Do NOT add features not in the current phase
- Do NOT skip tests for "simple" changes
- Do NOT let low-confidence AI change compliance status automatically

## 🛠 Commands
- `npm run dev` - Start dev server
- `npm run build` - Production build
- `npm run lint` - Lint (only when needed)
- `npm test` - Tests (if configured)
